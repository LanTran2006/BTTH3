<Window x:Class="Bai7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BÁN VÉ RẠP CHIẾU BÓNG" Height="420" Width="420" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

      
        <TextBlock Text="MÀN ẢNH"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="OrangeRed"
                   HorizontalAlignment="Center"
                   Margin="0,10,0,10"/>

       
        <UniformGrid x:Name="SeatGrid" Grid.Row="1" Rows="3" Columns="5" Margin="30">
          
        </UniformGrid>

      
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Center" Margin="0,10,0,10">
            <TextBlock Text="Thành tiền:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock x:Name="lblThanhTien" Width="100" VerticalAlignment="Center" Text="0" />
            <Button x:Name="btnChon" Content="Chọn" Width="70" Margin="8,0,0,0" Click="btnChon_Click"/>
            <Button x:Name="btnHuy" Content="Hủy bỏ" Width="70" Margin="8,0,0,0" Click="btnHuy_Click"/>
            <Button x:Name="btnKetThuc" Content="Kết thúc" Width="70" Margin="8,0,0,0" Click="btnKetThuc_Click"/>
        </StackPanel>
    </Grid>
</Window>
